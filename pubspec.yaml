name: run_walk_timer
description: Base Pacer, a focused run/walk interval timer for Android.
publish_to: 'none'
version: 1.3.0+7

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
